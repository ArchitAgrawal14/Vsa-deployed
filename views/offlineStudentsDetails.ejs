<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Student Details</title>
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        .featurette-heading {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
        .achievement-item {
            background-color: #fff;
            border-radius: 5px;
            padding: 15px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }
        .featurette-image {
            border-radius: 10px;
        }
        .no-achievements {
            font-style: italic;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <% if (studentDetails && studentDetails.length > 0) { %>
            <% studentDetails.forEach((studDetails) => { %>
                <div class="row featurette mb-5">
                    <div class="col-md-7">
                        <h2 class="featurette-heading fw-normal lh-1">
                            <%= studDetails.student_name %> 
                            <span class="text-body-secondary">(Smt <%= studDetails.mother_name %>, Shri <%= studDetails.father_name %>)</span>
                        </h2>
                        
                        <!-- Section to display achievements -->
                        <h3 class="mt-4">Achievements:</h3>
                        <div class="achievement-item">
                            <% if (studDetails.medals_won || studDetails.speciality || studDetails.description || studDetails.experience) { %>
                                <p><strong>Medals Won:</strong> <%= studDetails.medals_won || 'N/A' %></p>
                                <p><strong>Speciality:</strong> <%= studDetails.speciality || 'N/A' %></p>
                                <p><strong>Description:</strong> <%= studDetails.description || 'N/A' %></p>
                                <p><strong>Experience:</strong> <%= studDetails.experience || 'N/A' %></p>
                            <% } else { %>
                                <p class="no-achievements">No achievements listed.</p>
                            <% } %>
                        </div>
                    </div>
                    <div class="col-md-5 text-center">
                        <img class="bd-placeholder-img bd-placeholder-img-lg featurette-image img-fluid mx-auto" 
                             src="<%= studDetails.img %>" 
                             alt="Student Image" 
                             width="500" 
                             height="500" />
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="alert alert-warning" role="alert">
                No student details found.
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-Ob8eNeFxM4hYcU+kKZ4WvRoc6+sDrR6wPAX1y5eU9nq6VR7AWIk2qqD4U+ElQ/p5" crossorigin="anonymous"></script>
</body>
</html>
